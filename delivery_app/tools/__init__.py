"""Tools (acciones ejecutables) para la aplicación FastMCP.

Estos módulos definen las funciones `@app.tool()` que pueden ser
invocadas desde la interfaz de usuario. Operan usando las clases
de la capa de servicios (`trip_service`, etc.).
"""
